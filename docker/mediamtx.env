# Environment for the MediaMTX container in docker-compose.infra.yml.
#
# Tracked in git on purpose: no credentials here. The media plane is configured
# by mediamtx.yml, which is bind-mounted read-only into the container.

TZ=UTC
